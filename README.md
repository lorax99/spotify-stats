## Current TODOs

1. Configure Linter and Prettier.
2. Some state is not getting updated correctly, I'm currently stuck in a redirect loop. Probably we need to use the React Router hook to redirect us, we cannot just set the `document.location` inside the redirect function.
